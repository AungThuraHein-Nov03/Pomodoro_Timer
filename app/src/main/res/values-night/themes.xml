<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Force light appearance even in dark mode -->
    <style name="Base.Theme.MyApplicationPMT" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/pomodoro_primary</item>
        <item name="colorSecondary">@color/pomodoro_secondary</item>
        <item name="android:statusBarColor">@color/background_light</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@color/background_light</item>
    </style>
</resources>